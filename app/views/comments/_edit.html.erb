<% semantic_form_for comment do |form| %>
	<% form.inputs do %>
		<%= form.input :language, :include_blank => false %>
		<%= form.input :title %>
		<%= form.input :comment %>
		<%= form.input :commentable_id, :as => :hidden, :value => synonymy.id %>
	<% end %>
	<%= form.buttons %>
<% end %>
