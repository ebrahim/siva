<% content_for :head do %>
<%= siva_title :nav_domains, :form_show %>
<% end %>

<%= h (@parent_domain.nil? ? '' : @parent_domain.name + ' > ') + @domain.name %><br/>

<%= link_to t(:form_edit), edit_domain_path %>
<%= link_to t(:form_remove), @domain, :confirm => t(:domain_remove_confirm), :method => :delete%>
<%= link_to t(:form_index), domains_path %>
<%= link_to_back %>
