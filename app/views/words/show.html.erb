<% content_for :head do %>
<%= siva_title :nav_words, :form_show %>
<% end %>

<%= render 'show', :word => @word %>

<%= link_to t(:form_edit), edit_word_path if can_access?(:update, :words) %>
<%= link_to t(:form_remove), @word, :confirm => t(:word_remove_confirm), :method => :delete if can_access?(:destroy, :words) %>
<%= link_to t(:form_index), words_path %>
<%= link_to_back %>
